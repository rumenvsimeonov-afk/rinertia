[Unit]
Description=rinertia pointer inertia daemon for Linux touchpads
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/rinertia --config %h/.config/rinertia/config.toml
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
